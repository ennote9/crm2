# PR Checklist v1.0 — UI Redesign

Critical:
- No business-rule changes
- Actions only by available/disabled/ui_hints
- Tokens only (no random style values)
- Full width, no useless horizontal scroll
- Stats toggle doesn’t reset state; stats lazy-load
- Errors/W_* dialogs follow spec; focus works
- Keyboard focus visible; Ctrl/Cmd+F for search
- Light/Dark readable

Also:
- Grid selection + header checkbox + bulk bar
- Filters explicit apply + chips + clear all
- Views apply + custom state
- Mobile uses cards/flows, stats as separate screen
